import { LoginIndex } from "@/components/pages/login";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Login | Product Factory",
  description: "Login | Product Factory",
};

export default LoginIndex;
