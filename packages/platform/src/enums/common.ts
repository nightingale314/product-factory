export enum SortEnum {
  ASC = "asc",
  DESC = "desc",
}

export enum ServerErrorCode {
  SUCCESS = 0,
  UNAUTHORIZED = 100401,
  UNEXPECTED_ERROR = 100500,
  AUTH_INVALID_CREDENTIALS = 600400,
  AUTH_INVALID_CREDENTIALS_SCHEMA = 600404,
  ATTRIBUTE_CONFLICT = 200409,
  INVALID_RESOURCE_UPLOAD_REQUEST = 300400,
}
